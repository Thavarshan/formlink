/**
 * Form state enumeration for better state management
 */
export enum FormState {
  IDLE = 'idle',
  PROCESSING = 'processing',
  SUCCESS = 'success',
  ERROR = 'error',
  CANCELLED = 'cancelled'
}
